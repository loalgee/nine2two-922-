// nine2two — Supabase project credentials.
//
// Paste your own values here (SETUP.md steps 4–5):
//   SUPABASE_URL      → Supabase dashboard → Project Settings → Data API → Project URL
//   SUPABASE_ANON_KEY → same page → "anon public" API key
//
// The anon key is designed to be public — it ships to every browser that
// loads the app. Row Level Security in supabase/schema.sql is what actually
// protects the data, so committing this file is fine.

export const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
